/*
#include <iostream>
#include "NodeLinkedList.cpp"
using std::cin;
using std::cout;

typedef struct LinkedList{
    int size;
    node* Head;
}LinkedList;

int main(){
    LinkedList *BulletList= new LinkedList;
    BulletList->Head= Create(40);
    node* initial= BulletList->Head;
    initial=InsertAtEnd(initial,42);
    Traverse(initial);
    return 0;
}
*/
